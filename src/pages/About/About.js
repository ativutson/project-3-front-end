import { Link } from 'react-router-dom';

const About = (props) => {
    return (
        <>
            <h1>About Page</h1>
            <Link to="/">HOME</Link>
        </>
    );
};

export default About;